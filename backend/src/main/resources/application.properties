## POSTGRES LOCAL
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_DATABASE}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

## MSSQL
#spring.datasource.url=jdbc:sqlserver://localhost;databaseName=clinic
#spring.datasource.username=root
#spring.datasource.password=root
#spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
#spring.datasource.url=jdbc:sqlserver://clinic-server.database.windows.net:1433;database=clinic;user=clinicoperator@clinic-server;password={br0w@rp0dprych4};encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
#spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
#spring.jpa.hibernate.ddl-auto=create-drop

spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
server.error.include-stacktrace=never
jwt.header=Authorization
jwt.secret=${JWT_SECRET}
spring.jpa.show-sql=true
cloudinary.api.key=${CLOUDINARY_API_KEY}
cloudinary.api.secret=${CLOUDINARY_API_SECRET}
cloudinary.name=${CLOUDINARY_NAME}
logging.level.org.hibernate.SQL=DEBUG
cloudinary.url=${CLOUDINARY_URL}
# production client
client.url=${CLIENT_URL}
